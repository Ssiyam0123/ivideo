/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ik.imagekit.io', 
      'www.thumbline.com', 
      'www.firstone.com', 
      'www.secondone.com'
    ],
  },
};

module.exports = nextConfig;
